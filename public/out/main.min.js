"use strict";

!function () {
  "use strict";

  const e = {
    formEl: document.querySelector(".nav-form__post"),
    inputEl: document.querySelector(".nav-form__input"),
    loginBtn: document.querySelector(".nav-auth__link"),
    listEl: document.querySelector(".main__list"),
    spinEl: document.querySelector(".spin")
  };
  console.clear(), e.formEl.addEventListener("submit", t => {
    t.preventDefault();
    const n = e.inputEl.value.trim();
    console.log(n), localStorage.setItem("input", n), localStorage.getItem("input"), "" === n ? (e.inputEl.placeholder = "[error]", console.error("[error]")) : (async function (t) {
      await fetch("/", {
        method: "POST",
        headers: {
          "Content-type": "application/json"
        },
        body: JSON.stringify({
          track: t
        })
      }).then(e => e.json()).then(t => function (t) {
        null != t && e.spinEl.classList.add("is-hidden");
        const n = t.tracks.items;
        let l = e.listEl;
        l.innerHTML = "", n.forEach((e, t) => {
          const n = document.createElement("li"),
            o = document.createElement("span"),
            a = document.createElement("img"),
            i = document.createElement("a");
          o.classList.add("main__span"), n.classList.add("main__item"), a.classList.add("main__img"), i.classList.add("main__link"), a.src = e.album.images[1].url, a.alt = e.album.name, i.href = e.external_urls.spotify, i.target = "_blank", i.rel = "noopener noreferrer nofollow", i.textContent = "".concat(e.name, " - ").concat(e.artists[0].name), o.textContent = "click", l.appendChild(n), i.appendChild(o), n.appendChild(a), n.appendChild(i);
        });
      }(t)).catch(e => console.error(e));
    }(n), e.spinEl.classList.remove("is-hidden"), e.inputEl.placeholder = "Type to search..."), e.formEl.reset();
  }), e.loginBtn.addEventListener("click", () => {
    window.open("/auth/twitch");
  });
}();